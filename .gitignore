node_modules/
dist/
build/
.env
.env.local
*.local
coverage/
.DS_Store
Thumbs.db
*.log
.vite/
.turbo/
apps/server/data/
# The default DB_PATH is CWD-relative — running the server from the repo root (e.g. `npm start`,
# which is exactly what a one-service deploy does) creates this instead of apps/server/data/.
/data/

# Raw D&D Beyond exports may contain your account's userId/username — never commit unsanitized.
# The sanitized copies used for tests live under packages/domain/src/__fixtures__/.
dndbeyond-json-sample.md
encounter-roles.md
encounter-stats.md
complex-second-phase.md
*.dndbeyond-raw.json

